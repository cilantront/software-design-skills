# 2026-07-10

## 创建 CODEBUDDY.md
- 在仓库根新增 `CODEBUDDY.md`，记录「精选不通过 Demo 的处理规则」：① `demos-meta.md` 的「通过精选」列记 ❌、链接改回 `demos/<name>`；② `landing-page/index.html` 的 `CASES_DEMOS` 与 `ENDS_BY_ID` 移除对应条目并修正案例总数；③ 文件夹从 `landing-page/demos/<name>` 移回 `demos/<name>`。
- 该仓库为「优特云 Skills 推广与运营」静态落地页（Tailwind CDN，无构建/测试），案例漏斗状态由 `demos-meta.md` 跟踪（通过粗筛 / 通过精选 / 包括端类型）。
- 用户偏好：明确要求“别想太多”，只记录规则本身，避免过度展开架构。

## 补充 CODEBUDDY.md：原型「宫格展示」规则
- 新增「移动端 / 小程序」宫格展示规范（参考 `咖损管家-移动端原型.html` 与 `邻里拼车…/mini-program-prototype.html`）。
- 共同规则：单 HTML 多手机并排、屏幕统一 375×750、内层滚动、页面标题标签、文件名加 `-宫格展示`。
- 移动端：带 8px 厚边框真机外壳、iOS 状态栏、标题标签在手机下方、网格 `repeat(3,375px)` 居中。
- 小程序：无厚边框+白色圆角卡片外壳、微信绿 `#07c160` 状态栏/导航栏、home-indicator、标题标签在手机上方、响应式 `repeat(4,1fr)` 断点 1600/1200/800→3/2/1。
- 命名修正（用户二次指示）：**不设「-宫格展示」文件名规则**，改为「直接在原文件上修改」；仅原文件含多端需拆分时才按 `<产品名>-xxx端原型.html` 新建。章节标题定为「原型宫格展示重构规则」。

## Web 端原型铺满屏幕改造 + 规则入库
- 改造「宠物保险对比助手」2 个 Web 端原型：`admin-web-prototype.html`、`product-admin-web-prototype.html`。删除外围 `.site-header`（站点标题+WEB端说明），`body { margin:0; height:100vh; overflow:hidden }`，`.admin-demo` 改 `width:100%; height:100vh; flex column`，`.admin-body { flex:1; min-height:0 }`，`.admin-content { overflow-y:auto }` 仅内部滚动。参考 `客服知识库缺口捕捉器/kb-gap-catcher-prototype.html`。
- 注意：原型目录中 `admin-web/`、`product-admin-web/` 子文件夹内是 widget 级 html（data-stats-widget 等），非主原型，未动。
- CODEBUDDY.md 新增「⭐ Web 端原型铺满屏幕规则（后台管理类）」：去外围标题块、body 撑满、容器占满、内部滚动、保留产品内顶栏。

## CODEBUDDY.md 小程序宫格规则精简（用户二次指示）
- 删除小程序节「主色统一微信绿 #07c160」整条配色规则；并去掉「状态栏一体化」的 `background:#07c160`（微信绿）与「标题标签」的「绿色」字样 → 小程序原型不再强制微信绿配色。
- 删除小程序节「底部 home-indicator（黑底横条）」整条。
- 保留：无厚边框外壳、状态栏+微信导航结构、标题标签放上方、响应式 4 列网格。

## 宠物保险对比助手 移动端/小程序 宫格微调
- 移动端 `c-end-h5-prototype.html`：状态栏默认与 `.nav-bar` 默认由橙色渐变 #FF8C42→#FF6B35 改为白色 iOS 风（status-bar `background:#fff; color:#333`；nav-bar `background:#fff; border-bottom:1px solid #f0f0f0; color:#333`）。`.phone` 厚边框外壳保留（移动端规范）。
- 小程序 `c-end-miniapp-prototype.html`：外壳由 `.phone`(8px厚边框,圆角40px) 改为 `.phone-screen`(圆角12px,无厚边框)+ 外套白色圆角卡片 `.phone-wrapper`(padding20px,圆角20px)。因 20px 留白会使固定 3×375px 网格溢出，同步将网格改为小程序响应式 `repeat(4,1fr); max-width:1800px` + 断点 1600/1200/800→3/2/1。
- 小程序剩余未改项（用户仅要求本次改外壳）：状态栏+微信导航一体化、标题标签置顶。

## 网格布局统一（移动端+小程序）
- 用户指示统一网格：参考小程序布局，最多 3 列、随视口收窄折叠、断点 `1200/800px` 降为 `2/1` 列。
- 两文件 `.screen-grid` 统一为 `repeat(3, 1fr); max-width:1800px; margin:0 auto; gap:30px` + 媒体查询 `1200px→2列`、`800px→1列`。
- 移动端 `.phone` 加 `margin:0 auto`（固定 375px，列内居中）；小程序 `.phone-wrapper` 设 `width:415px; margin:0 auto`（列内居中，避免 1fr 拉伸）。
- CODEBUDDY.md 移动端/小程序两节「网格布局」同步改为统一描述。

## 小程序外壳回调为移动端 .phone（沿用 line49）
- 用户要求：小程序端外壳设计/尺寸沿用 CODEBUDDY.md 移动端 line49（`.phone` 厚边框 8px、圆角40px），不再用 `.phone-screen` + `.phone-wrapper`。
- 小程序 `c-end-miniapp-prototype.html`：CSS `.phone-wrapper`/`.phone-screen` 两条合并为单一 `.phone`（尺寸 375×750、border:8px solid #1a1a1a、圆角40px、阴影，与 H5 一致）；标记 `<phone-wrapper><phone-screen>` 批量还原为单 `<phone>`，去除多余闭合 `</div>`（已校验 12 个 `.phone`、0 个 wrapper/screen 残留）。
- CODEBUDDY.md 小程序节 line56 改为「外壳带边框（同移动端 line49）」，网格布局居中说明 `.phone-wrapper` → `.phone`。
- 注：因外壳改回 `.phone`，标题标签现也落在手机下方（与移动端一致）。

## 宠物保险对比助手 精选不通过（三步）
- 原因：web 全站页面的侧边栏功能只实现了一个，没有全部功能原型。
- Step1 meta：`demos-meta.md` 该行「通过精选」记 `❌`、`通过粗筛` 保留 `✅`、链接路径改回 `demos/宠物保险对比助手`。
- Step2 移除卡片：`landing-page/index.html` 从 `CASES_DEMOS` 删除 `pet-insurance` 对象、从 `ENDS_BY_ID` 删除 `'pet-insurance'` 映射、顶部「63 个案例」→「62 个案例」。全文搜索 `pet-insurance`/`宠物保险对比助手` 残留 = 0。
- Step3 移回资料：`landing-page/demos/宠物保险对比助手/` 已 Move-Item 回 `demos/宠物保险对比助手/`（含 assets 15 html + URS/PRD md）。

## 儿童兴趣班接送交接表 全套样式标准化（web + 小程序）
- 范围：用户确认「小程序 + Web 都改（全套样式标准化）」。
- 小程序（teacher/parent 两个文件）：
  - 网格 `repeat(auto-fit, minmax(320px,1fr)); max-width:1400px` → 统一 `repeat(3,1fr); max-width:1800px; margin:0 auto; gap:30px`（与移动端一致；无单独断点则沿用全站 1200/800→2/1 约定）。
  - 外壳 `.phone-frame`（圆角40px 无厚边框）→ `.phone`：width375/height750、border:8px solid #1a1a1a、border-radius40px、box-shadow、overflow hidden、flex column；`.phone-content` 由 `height:600px` 改 `flex:1; min-height:0; overflow-y:auto`（屏内滚动）。标记 `class="phone-frame"`→`class="phone"`（teacher 8处、parent 7处，0残留）。保留品牌色 `.phone-header`（蓝/绿）与屏内 `.page-label`。
  - 小程序 `.prototype-title`/`.prototype-subtitle` 站点标题：**保留**（铺满全屏规则仅针对 Web；与宠物保险对比助手小程序处理一致）。
- Web 管理后台（`admin-web-prototype.html`，4 个页面）：
  - 移除站点级 `.prototype-title`/`.prototype-subtitle` 的 CSS 与 body 顶部 h1/p（铺满规则：去外围装饰标题）。
  - body 加 `margin:0`；`.admin-container` 由 `min-height:600px; max-width:1400px; margin:0 auto 30px; border-radius; box-shadow` 改为 `width:100%; height:100vh; overflow:hidden`（每页铺满视口，4 页纵向滚动）；保留侧边栏+顶栏+`.admin-content` 屏内滚动。

## CODEBUDDY.md 新增「Web 端原型单页应用（SPA）」规则
- 用户要求：给 web 原型追加规则——必须以单页应用方式展示全站设计，参考「客服知识库缺口捕捉器」。
- 位置：在「Web 端原型铺满屏幕规则」之后新增 `## ⭐ Web 端原型「单页应用（SPA）」规则（全站设计）` 一节，引用 `kb-gap-catcher-prototype.html` 为参考范例。
- 要点：① 单一应用外壳（侧边栏+.main-content）；② 各功能页为 `.page-section`（CSS `display:none`/`.active{display:block}`），侧边栏 `nav-item` `onclick="switchPage('xxx', this)"` 切换；③ 登录/注册入口页也纳入壳内通过 JS（如 `doLogin()`）进入主壳；④ 与铺满规则叠加（body 100vh、外壳铺满、内容区屏内滚动，不纵向长滚罗列多页）；⑤ 不保留外围装饰标题。
- 注：此规则为撰写规范，尚未对现有 demo 的 web 原型做 SPA 化改造（如儿童兴趣班接送交接表 admin-web 当前仍是 4 页纵向长滚，需后续改造）。

## 儿童兴趣班接送交接表 admin-web 重构为 SPA
- 将 `admin-web-prototype.html` 由「4 个独立 `.admin-container` 纵向长滚」重构为**单文件单页应用（SPA）**，遵循新加的 Web SPA 规则、参考 kb-gap-catcher。
- 结构：单一外壳（`.admin-sidebar` 侧边栏 + `.admin-header` 全局顶栏 + `.admin-content`）；原 4 页内容包成 `<section class="page-section" id="page-xxx">`，仅 `.active` 显示。
- 侧边栏 7 个 `sidebar-menu-item`（data-page + `onclick="switchPage(...)"`）：dashboard(工作台,默认active)、org/classes/teachers/settings 为占位页、students(学生管理)、records(记录查询)；student-detail(学生详情) 由学生列表「查看」按钮 `switchPage('student-detail', ...)` 进入。
- JS `switchPage(pageId, navEl)`：隐藏全部 `.page-section`→显示 `page-<id>`→同步侧边栏高亮→更新 `#breadcrumb` 面包屑（BREADCRUMBS 映射）。
- 移除原每页的 `.page-label` 装饰条（SPA 由面包屑表达页面），删除对应 `.page-label`/`.page-label-tag` CSS；body 维持 `100vh; overflow:hidden`，内容区屏内滚动。
- 校验：全文仅 1 个 `.admin-container`、8 个 `.page-section`、`page-label` 残留 0。

## 儿童兴趣班接送交接表 精选通过
- 该 demo 此前已在 `landing-page/demos/` 且已在 `index.html`（`children-class-shuttle`，CASES_DEMOS 行907 + ENDS_BY_ID 行1753），仅 meta「通过精选」列为空（待精选）。因此「精选通过」**无需**走完整逆向三步（勿重复移动/补回/+1）。
- 唯一动作：`demos-meta.md` 第35行李「通过精选」列由空 → `✅`，链接本就指向 `landing-page/demos/`，「通过粗筛」保持 `✅`。
- 校验：案例总数仍为 `62 个案例`（已含该 demo，无重复计数）；`demos/` 下无该 demo 副本（仅 landing-page/demos 一份）。

## 交付前资料风险确认台 精选通过
- 同前：该 demo 此前已在 `landing-page/demos/`（及 `index.html` 的 `delivery-risk-check`，CASES_DEMOS 行921 + ENDS_BY_ID 行1754），仅 meta「通过精选」列为空（待精选）。故「精选通过」**无需**走完整逆向三步（勿重复移动/补回/+1）。
- 唯一动作：`demos-meta.md` 第41行李「通过精选」列由空 → `✅`，链接本就指向 `landing-page/demos/`，「通过粗筛」保持 `✅`。
- 校验：案例总数仍为 `62 个案例`（已含该 demo，无重复计数）；`demos/` 下无该 demo 副本。

## 客服反馈转产品需求卡AI 清理散装页面
- 该 demo 已有完整 SPA：`landing-page/demos/客服反馈转产品需求卡AI/UI-客服反馈转产品需求卡AI.html`（单文件含首页/数据导入/需求卡片/周报 4 页切换）。原先 4 个散装页面已无存在必要，用户要求删除。
- 删除 4 个散装 HTML：`cards.html`(卡片看板)、`import.html`(数据导入)、`index.html`(首页/仪表盘)、`report.html`(周报)。保留 `UI-...html` + URS/PRD md。
- `landing-page/index.html` 的 `cs-feedback-to-card`（行933）`prototypes` 数组原为 4 条（需求卡片管理=SPA + 数据导入/报表分析/卡片看板 散装），改为仅 1 条 `{ label:"全站原型", url: UI-客服反馈转产品需求卡AI.html }`。
- 校验：删除后目录仅剩 `UI-...html` + 2 md；`index.html` 对 `cards/import/report/index.html` 的引用 0；SPA 内部未引用这些散装文件。
- 备注：`URS-客服反馈转产品需求卡AI.md` 第67-70 行的「原型路径」表仍列旧散装文件名（./.../index.html 等），已成失效链接，未改动（文档性质，是否同步更新待用户决定）。

## 客服反馈转产品需求卡AI 精选通过
- 同前两例：该 demo 已在 `landing-page/demos/` 且已在 `index.html`（`cs-feedback-to-card`），仅 meta「通过精选」列为空（待精选）。
- 唯一动作：`demos-meta.md` 第42行「通过精选」列由空 → `✅`，链接/「通过粗筛」保持不变。无需移动/补回/+1（案例数仍 62）。

## 客服知识库缺口捕捉器 精选通过
- 同前：已在 `landing-page/demos/` 且已在 `index.html`（`kb-gap-catcher`，本身即 SPA 参考范例），`demos/` 无副本。
- 唯一动作：`demos-meta.md` 第43行「通过精选」列由空 → `✅`；链接/「通过粗筛」不变；无移动/补回/+1（案例数仍 62）。
